# Canonical comparator boundary

This directory is reserved for a comparator that accepts two canonical result objects and reports
the first semantic JSON-path mismatch. It must not execute fixtures, contact providers, or apply
runner-specific exceptions. See [`../runners.md`](../runners.md) and
[`../normalization.md`](../normalization.md).

