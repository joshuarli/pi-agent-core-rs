# Normalized runner output

Runners may write one canonical result per fixture here while a parity run is in progress. These
files are derived artifacts, not fixture inputs or golden truth. They must conform to
[`../../normalization.md`](../../normalization.md); promote a result to `expected/` only as an
intentional, reviewed contract change.

