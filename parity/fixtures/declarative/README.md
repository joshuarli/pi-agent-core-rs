# Declarative fixtures

Put provider-free, deterministic input cases here. Each JSON file has
`kind: "declarative_parity_fixture"`; see [`../../fixture-format.md`](../../fixture-format.md).
The directory must remain safe to execute with network and ambient filesystem access disabled.

