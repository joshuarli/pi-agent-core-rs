# Rust runner boundary

This directory is reserved for the runner that calls the pure Rust agent library directly. The
scaffold intentionally contains no crate or Cargo manifest. The runner contract is documented in
[`../runners.md`](../runners.md).

